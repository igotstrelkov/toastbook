import { defineSchema } from "convex/server"

// Stage 0: empty schema. The canonical tables (users, events, recordings)
// land in Stage 2 — see docs/stages-1-2-detailed.md.
export default defineSchema({})
